/* Crear una función que convierta los strings que se ingresan como parámetros a estilo capitalizado.
Ejemplo: "hola a todos" => "Hola A Todos" */

let frases = "hola a todos los humanos"

function capitalizar(frase){
    let fraseCortada = frase.split("") /* El método split() divide un objeto de tipo String en un array (vector) de cadenas mediante la separación de la cadena en subcadenas. */
    let newAarray = []

    for(let i = 0; < fraseCortada.length ;i++){
        
    }
}